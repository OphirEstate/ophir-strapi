name: Keep Strapi Alive

  on:
    schedule:
      - cron: '*/10 * * * *'
    workflow_dispatch:

  jobs:
    ping:
      runs-on: ubuntu-latest
      steps:
        - name: Ping Strapi
          run: curl -s https://ophir-strapi.onrender.com
